# Gui-Calculator
This program allow us to run a simple calculator with a simple graphical user interface( GUI). 
The code provide a simple introduction to python Tkinter package as well as simple GUI in-built functions such as StringVar(), Buttons, Frame, pack(), grid(), etc....
When running, the calculator is able to compute simple mathmatics.
